// Stubbed by scripts/rebrand.ts — no native review prompt in the classroom web build.
export function useAppReview() {
  return {
    maybeRequestReview: async (_totalCompleted: number) => {},
  };
}
