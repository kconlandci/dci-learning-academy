import type { LabManifest } from "../../types/manifest";

export const cloudDlpConfigurationLab: LabManifest = {
  schemaVersion: "1.1",
  id: "cloud-dlp-configuration",
  version: 1,
  title: "Cloud DLP Configuration",
  tier: "intermediate",
  track: "cloud-security",
  difficulty: "challenging",
  accessLevel: "free",
  tags: ["security", "dlp", "data-loss-prevention", "macie", "pii", "data-classification"],
  description:
    "Configure cloud data loss prevention controls to detect, classify, and protect sensitive data across storage, data pipelines, and egress channels. Prevent PII, PHI, and financial data from leaving the organization uncontrolled.",
  estimatedMinutes: 18,
  learningObjectives: [
    "Configure Amazon Macie to detect and classify sensitive data in S3 buckets",
    "Build DLP policies that block or alert on sensitive data in egress channels",
    "Define custom data identifiers for organization-specific sensitive data patterns",
    "Integrate DLP findings with automated remediation workflows",
    "Balance DLP enforcement stringency against business operational impact",
  ],
  sortOrder: 510,
  status: "published",
  prerequisites: [],
  rendererType: "toggle-config",
  scenarios: [
    {
      type: "toggle-config",
      id: "dlp-s1-macie-setup",
      title: "Amazon Macie Sensitive Data Discovery Configuration",
      description:
        "Your organization stores data across 340 S3 buckets. Amazon Macie is being configured for the first time. Set up the discovery job and classification settings to provide meaningful sensitive data coverage without generating alert fatigue.",
      targetSystem: "Amazon Macie: Initial Configuration (us-east-1)",
      items: [
        {
          id: "i1",
          label: "Discovery Job Scope",
          detail: "Which S3 buckets to include in the sensitive data discovery scan.",
          currentState: "No buckets selected",
          correctState: "All S3 buckets with tags: data-classification=sensitive OR data-classification=restricted",
          states: [
            "No buckets selected",
            "All 340 S3 buckets",
            "All S3 buckets with tags: data-classification=sensitive OR data-classification=restricted",
            "Only buckets prefixed with 'prod-'",
          ],
          rationaleId: "r1",
        },
        {
          id: "i2",
          label: "Managed Data Identifiers",
          detail: "Built-in patterns Macie uses to identify PII, financial, and healthcare data.",
          currentState: "Default only (US SSN, Credit Card)",
          correctState: "All managed identifiers relevant to business (PII, PHI, financial, credentials)",
          states: [
            "Default only (US SSN, Credit Card)",
            "All managed identifiers relevant to business (PII, PHI, financial, credentials)",
            "All identifiers including international formats",
            "No managed identifiers (custom only)",
          ],
          rationaleId: "r2",
        },
        {
          id: "i3",
          label: "Discovery Job Schedule",
          detail: "How frequently Macie scans for sensitive data changes.",
          currentState: "One-time scan",
          correctState: "Daily incremental scan",
          states: ["One-time scan", "Weekly full scan", "Daily incremental scan", "Monthly full scan"],
          rationaleId: "r3",
        },
        {
          id: "i4",
          label: "Finding Publication to Security Hub",
          detail: "Publishes Macie findings to AWS Security Hub for centralized alert management.",
          currentState: "Disabled",
          correctState: "Enabled",
          states: ["Disabled", "Enabled"],
          rationaleId: "r4",
        },
        {
          id: "i5",
          label: "Suppression Rules for Known False Positives",
          detail: "Auto-archive findings that match known benign data patterns (e.g., test data with fake SSNs).",
          currentState: "No suppression rules",
          correctState: "Suppression rule for buckets tagged: environment=test AND data-type=synthetic",
          states: [
            "No suppression rules",
            "Suppression rule for buckets tagged: environment=test AND data-type=synthetic",
            "Suppress all MEDIUM severity findings",
            "Suppress all findings older than 30 days",
          ],
          rationaleId: "r5",
        },
      ],
      rationales: [
        {
          id: "r1",
          text: "Scoping to tagged sensitive/restricted buckets focuses Macie on the highest-risk data stores. Scanning all 340 buckets including dev and test generates noise and cost without proportional risk reduction.",
        },
        {
          id: "r2",
          text: "Enabling all business-relevant managed identifiers ensures Macie detects the full range of regulated data your organization handles — SSN, credit card numbers, PHI identifiers, and AWS credentials.",
        },
        {
          id: "r3",
          text: "Daily incremental scans detect new sensitive data within 24 hours of upload, balancing coverage timeliness against the cost of full weekly rescans.",
        },
        {
          id: "r4",
          text: "Publishing to Security Hub centralizes DLP findings with other security alerts, enabling correlation with GuardDuty and Inspector findings in a single pane of glass.",
        },
        {
          id: "r5",
          text: "Suppressing known test data buckets with synthetic PII eliminates a major source of false positives, allowing analysts to focus on genuine findings without tuning out the entire Macie feed.",
        },
      ],
      feedback: {
        perfect: "Macie correctly configured with scoped coverage, comprehensive identifiers, daily incremental scanning, Security Hub integration, and false positive suppression.",
        partial: "Most settings are correct but check the discovery scope and false positive suppression. Alert fatigue from scanning test buckets can cause analysts to ignore real findings.",
        wrong: "The Macie configuration is missing critical settings. Without proper scope, managed identifiers, and Security Hub integration, DLP coverage will be incomplete and findings unactionable.",
      },
    },
    {
      type: "toggle-config",
      id: "dlp-s2-egress-controls",
      title: "Data Egress DLP Policy Configuration",
      description:
        "The security team needs to prevent sensitive data from leaving the organization through unapproved egress channels. Configure DLP controls on the key egress paths: S3 public access, email relay, and API responses.",
      targetSystem: "Organizational DLP Policy: Egress Controls",
      items: [
        {
          id: "i1",
          label: "S3 Organization-Level Block Public Access",
          detail: "AWS Organizations policy that prevents any S3 bucket in the account from being made public.",
          currentState: "Not configured",
          correctState: "Enabled via AWS Organizations SCP",
          states: ["Not configured", "Enabled via AWS Organizations SCP", "Enabled per-account manually"],
          rationaleId: "r1",
        },
        {
          id: "i2",
          label: "API Response Sensitive Data Filtering",
          detail: "Scans API Gateway responses for sensitive data patterns before delivery to clients.",
          currentState: "No filtering",
          correctState: "Enable response transformation to mask SSNs and card numbers in API responses",
          states: ["No filtering", "Block all responses containing sensitive data", "Enable response transformation to mask SSNs and card numbers in API responses"],
          rationaleId: "r2",
        },
        {
          id: "i3",
          label: "SES Email DLP Policy",
          detail: "Scans outbound emails sent via Amazon SES for sensitive data attachments.",
          currentState: "No scanning",
          correctState: "Scan attachments; quarantine emails containing unencrypted SSN or card numbers",
          states: ["No scanning", "Scan attachments; quarantine emails containing unencrypted SSN or card numbers", "Block all emails with attachments"],
          rationaleId: "r3",
        },
        {
          id: "i4",
          label: "VPC Endpoint for S3 Access",
          detail: "Routes S3 traffic through a VPC endpoint, enabling endpoint policy enforcement.",
          currentState: "Not configured (traffic via internet gateway)",
          correctState: "Enabled with endpoint policy restricting access to organization S3 buckets only",
          states: ["Not configured (traffic via internet gateway)", "Enabled (no endpoint policy)", "Enabled with endpoint policy restricting access to organization S3 buckets only"],
          rationaleId: "r4",
        },
      ],
      rationales: [
        {
          id: "r1",
          text: "An SCP enforcing S3 Block Public Access at the organization level is a preventive control that cannot be overridden by any account administrator — it's stronger than per-account settings.",
        },
        {
          id: "r2",
          text: "Masking sensitive data in API responses (replacing digits with * except last 4) allows APIs to function while preventing full sensitive data exposure in logs and client applications.",
        },
        {
          id: "r3",
          text: "Quarantining emails with unencrypted PII allows security review before delivery, preventing accidental exfiltration while not blocking all email with attachments.",
        },
        {
          id: "r4",
          text: "A VPC endpoint policy restricting access to only organization-controlled S3 buckets prevents data exfiltration to attacker-controlled S3 buckets even if IAM credentials are compromised.",
        },
      ],
      feedback: {
        perfect: "All egress DLP controls correctly configured. Organization-wide S3 blocking, API response masking, email DLP, and VPC endpoint policies provide defense-in-depth for data exfiltration prevention.",
        partial: "Some controls are correct but check the VPC endpoint configuration. Without an endpoint policy, a VPC endpoint still allows traffic to any S3 bucket including attacker-controlled ones.",
        wrong: "Multiple egress channels remain unprotected. Without org-level S3 public access blocking and VPC endpoint policies, sensitive data can be exfiltrated via S3 even with other controls in place.",
      },
    },
    {
      type: "toggle-config",
      id: "dlp-s3-custom-identifiers",
      title: "Custom Data Identifier Creation",
      description:
        "Your organization uses internal employee IDs (format: EMP-XXXXX) and customer contract numbers (format: CTR-YYYY-XXXXXXXX) that Macie's built-in identifiers don't detect. Configure custom data identifiers to extend DLP coverage to these organization-specific patterns.",
      targetSystem: "Amazon Macie: Custom Data Identifiers",
      items: [
        {
          id: "i1",
          label: "Employee ID Pattern",
          detail: "Regular expression to match internal employee IDs in the format EMP-12345.",
          currentState: "Not defined",
          correctState: "Regex: \\bEMP-[0-9]{5}\\b with keyword: employee",
          states: ["Not defined", "Regex: EMP.* (too broad)", "Regex: \\bEMP-[0-9]{5}\\b with keyword: employee", "Regex: [0-9]{5} (too generic)"],
          rationaleId: "r1",
        },
        {
          id: "i2",
          label: "Contract Number Pattern",
          detail: "Regular expression to match customer contract numbers in the format CTR-2026-12345678.",
          currentState: "Not defined",
          correctState: "Regex: \\bCTR-[0-9]{4}-[0-9]{8}\\b",
          states: ["Not defined", "Regex: CTR-.* (too broad)", "Regex: \\bCTR-[0-9]{4}-[0-9]{8}\\b", "Keyword search: contract number"],
          rationaleId: "r2",
        },
        {
          id: "i3",
          label: "Custom Identifier Severity",
          detail: "The severity level assigned to findings generated by these custom identifiers.",
          currentState: "Not set",
          correctState: "MEDIUM for employee IDs; HIGH for contract numbers (contractual obligation)",
          states: ["Not set", "LOW for both", "MEDIUM for employee IDs; HIGH for contract numbers (contractual obligation)", "CRITICAL for both"],
          rationaleId: "r3",
        },
        {
          id: "i4",
          label: "Custom Identifier Validation",
          detail: "How to test the custom identifier patterns before enabling in production scans.",
          currentState: "Deploy directly to production scan",
          correctState: "Test against sample data with known matches and known non-matches first",
          states: ["Deploy directly to production scan", "Test against sample data with known matches and known non-matches first", "Skip testing — patterns are simple enough"],
          rationaleId: "r4",
        },
      ],
      rationales: [
        {
          id: "r1",
          text: "Word boundaries (\\b) and keyword context prevent false positives. A bare EMP.* regex would match any string starting with EMP, generating thousands of false positives.",
        },
        {
          id: "r2",
          text: "An exact format regex with length constraints (4-digit year, 8-digit sequence) precisely matches contract numbers while rejecting partial matches that share substrings.",
        },
        {
          id: "r3",
          text: "Severity should reflect business impact. Contract number exposure could violate NDA clauses — HIGH severity is appropriate. Employee IDs are sensitive but lower impact — MEDIUM.",
        },
        {
          id: "r4",
          text: "Testing custom identifiers against a curated sample file before production deployment catches regex errors, false positive sources, and false negatives before they affect live scans.",
        },
      ],
      feedback: {
        perfect: "Custom identifiers correctly configured with precise regex patterns, appropriate severity levels, and validation testing. These patterns will detect organization-specific sensitive data reliably.",
        partial: "The pattern precision needs improvement. Overly broad regex patterns (EMP.*) generate excessive false positives that undermine analyst trust in DLP findings.",
        wrong: "Custom identifier patterns must be precise. Generic regex without word boundaries or length constraints creates alert fatigue from false positives, causing analysts to ignore the DLP feed entirely.",
      },
    },
  ],
  hints: [
    "Amazon Macie costs are based on the amount of data scanned — use bucket tags and scoping rules to focus coverage on regulated data buckets rather than scanning all S3 storage indiscriminately.",
    "VPC endpoint policies for S3 can restrict access to only buckets in your organization's AWS accounts — this prevents the 'data exfiltration to attacker's S3 bucket' attack even with compromised credentials.",
    "Custom Macie data identifiers support regex with up to 500 characters and optional keyword context — always use word boundaries (\\b) and test against both matching and non-matching samples before enabling in production.",
  ],
  scoring: {
    maxScore: 100,
    hintPenalty: 5,
    penalties: { perfect: 0, partial: 10, wrong: 20 },
    passingThresholds: { pass: 80, partial: 50 },
  },
  careerInsight:
    "Data Loss Prevention configuration is a key requirement for privacy and compliance engineering roles. DLP architects who understand both the technical controls (Macie, VPC endpoints, SES scanning) and the business context of data classification are essential at regulated enterprises.",
  toolRelevance: ["Amazon Macie", "AWS Organizations SCPs", "Amazon SES", "AWS API Gateway", "VPC Endpoints", "AWS Security Hub", "Google Cloud DLP"],
  createdAt: "2026-03-28",
  updatedAt: "2026-03-28",
};
