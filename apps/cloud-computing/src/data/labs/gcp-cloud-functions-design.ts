import type { LabManifest } from "../../types/manifest";

export const gcpCloudFunctionsDesignLab: LabManifest = {
  schemaVersion: "1.1",
  id: "gcp-cloud-functions-design",
  version: 1,
  title: "Cloud Functions Design Decisions",
  tier: "beginner",
  track: "gcp-essentials",
  difficulty: "easy",
  accessLevel: "free",
  tags: ["gcp", "cloud-functions", "serverless", "event-driven", "pub-sub", "cold-start"],
  description:
    "Make key design decisions for Cloud Functions deployments including trigger selection, memory/timeout configuration, cold start mitigation, and choosing between Cloud Functions vs Cloud Run.",
  estimatedMinutes: 10,
  learningObjectives: [
    "Select the appropriate trigger type (HTTP, Pub/Sub, Cloud Storage, Firestore) for a given use case",
    "Configure memory allocation and timeout values based on workload characteristics",
    "Identify when cold starts are a problem and apply minimum instance strategies",
    "Decide when Cloud Functions is the right tool vs Cloud Run or App Engine",
  ],
  sortOrder: 303,
  status: "published",
  prerequisites: [],
  rendererType: "action-rationale",
  scenarios: [
    {
      type: "action-rationale",
      id: "cf-scenario-1",
      title: "Image Processing Pipeline Trigger",
      context:
        "Your team needs to automatically generate thumbnails whenever a user uploads a profile image to Cloud Storage bucket 'user-uploads-prod'. The thumbnail generation function takes about 4 seconds and uses a native image library. The upload rate is ~500 images/hour during business hours, negligible overnight.",
      displayFields: [
        { label: "Trigger Event", value: "Object finalized in user-uploads-prod", emphasis: "normal" },
        { label: "Expected Processing Time", value: "~4 seconds per image", emphasis: "normal" },
        { label: "Upload Rate (peak)", value: "~500 images/hour", emphasis: "normal" },
        { label: "Upload Rate (off-peak)", value: "<5 images/hour", emphasis: "normal" },
        { label: "Library Dependency", value: "libvips (native C library)", emphasis: "warn" },
      ],
      actions: [
        { id: "gcs-trigger", label: "Deploy Cloud Function with Cloud Storage trigger (object.finalize)", color: "green" },
        { id: "http-trigger-webhook", label: "Deploy Cloud Function with HTTP trigger, call from upload API", color: "yellow" },
        { id: "pubsub-manual", label: "Publish to Pub/Sub from upload code, trigger function from Pub/Sub", color: "blue" },
        { id: "cloud-run-job", label: "Deploy as a Cloud Run Job triggered by Cloud Scheduler every 5 minutes", color: "orange" },
        { id: "cloud-run-service", label: "Deploy as a Cloud Run Service with Pub/Sub push subscription", color: "red" },
      ],
      correctActionId: "gcs-trigger",
      rationales: [
        { id: "r-gcs-native", text: "The Cloud Storage trigger fires directly on object.finalize events — no intermediary needed. This is the canonical pattern for GCS-triggered processing." },
        { id: "r-http-coupling", text: "An HTTP trigger requires the upload service to make a synchronous HTTP call, creating tight coupling and adding latency to the user upload path." },
        { id: "r-pubsub-redundant", text: "Adding a manual Pub/Sub step is unnecessary indirection — GCS already integrates natively with Cloud Functions via Eventarc triggers." },
        { id: "r-run-job-batch", text: "Cloud Run Jobs are for batch workloads that run on a schedule. Real-time per-upload processing requires event-driven execution, not polling every 5 minutes." },
        { id: "r-libvips-ok", text: "libvips works fine in Cloud Functions Gen 2 (which uses Docker containers). Memory should be set to 512 MB–1 GB to handle image buffers." },
      ],
      correctRationaleId: "r-gcs-native",
      feedback: {
        perfect: "Exactly right! Cloud Storage triggers are the most direct and efficient pattern for this use case. The function auto-scales with upload rate and costs nothing when idle.",
        partial: "Your approach works but adds unnecessary complexity. The Cloud Storage trigger provides native integration without extra services or coupling.",
        wrong: "The event source is a GCS object upload — Cloud Functions has native support for this via the object.finalize event. Use the built-in trigger rather than adding indirection.",
      },
    },
    {
      type: "action-rationale",
      id: "cf-scenario-2",
      title: "Cold Start Latency Complaint",
      context:
        "A customer-facing API is backed by a Cloud Function (Python 3.12, 256 MB, 0 minimum instances). During low-traffic periods at night, the first request after a cold period takes 3–5 seconds — causing timeout errors for mobile clients. The function handles authentication token validation and must respond within 1 second.",
      displayFields: [
        { label: "Function Runtime", value: "Python 3.12", emphasis: "normal" },
        { label: "Memory", value: "256 MB", emphasis: "normal" },
        { label: "Minimum Instances", value: "0", emphasis: "critical" },
        { label: "Cold Start Duration", value: "3–5 seconds", emphasis: "critical" },
        { label: "Response SLA", value: "< 1 second", emphasis: "warn" },
        { label: "Invocation Rate (off-peak)", value: "~10 req/hour", emphasis: "normal" },
      ],
      actions: [
        { id: "set-min-instances-1", label: "Set minimum instances to 1 to keep a warm instance alive", color: "green" },
        { id: "increase-memory", label: "Increase memory to 2 GB to reduce initialization time", color: "yellow" },
        { id: "switch-to-node", label: "Rewrite function in Node.js to reduce cold start duration", color: "blue" },
        { id: "http-keep-alive-ping", label: "Set up Cloud Scheduler to ping the function every 5 minutes", color: "orange" },
        { id: "migrate-cloud-run", label: "Migrate to Cloud Run with min-instances=1 and HTTP/2", color: "red" },
      ],
      correctActionId: "set-min-instances-1",
      rationales: [
        { id: "r-min-instances-direct", text: "Setting min-instances=1 keeps one function instance warm at all times, eliminating cold starts completely. The cost is minimal: one idle instance billed at ~$5–10/month." },
        { id: "r-memory-cold-start", text: "More memory can slightly reduce cold start time by allocating more CPU, but it does not eliminate the cold start — it just makes it faster. It won't reliably meet a 1-second SLA." },
        { id: "r-node-marginal", text: "Node.js cold starts are faster than Python (typically 200–500ms vs 1–2s), but a 3–5 second cold start suggests heavy imports. Runtime switch is a partial improvement, not a fix." },
        { id: "r-scheduler-ping", text: "Pinging the function every 5 minutes is a workaround that may still result in cold starts between pings. Cloud Functions auto-scales down instances after ~15 minutes of inactivity." },
        { id: "r-cloud-run-overkill", text: "Cloud Run is a valid option but overkill for a simple function. The correct Cloud Functions fix (min-instances) is simpler and achieves the same result." },
      ],
      correctRationaleId: "r-min-instances-direct",
      feedback: {
        perfect: "Perfect! min-instances=1 is the purpose-built solution for eliminating cold starts in latency-sensitive Cloud Functions. It's cheap, simple, and fully eliminates the problem.",
        partial: "Your choice reduces cold start duration but doesn't eliminate it. The 1-second SLA requires guaranteed warm instances, not faster cold starts.",
        wrong: "The root cause is min-instances=0 — GCP shuts down all instances during low traffic, causing cold starts on the next request. Set min-instances=1 to keep one warm.",
      },
    },
    {
      type: "action-rationale",
      id: "cf-scenario-3",
      title: "Cloud Functions vs Cloud Run Decision",
      context:
        "A team wants to deploy a new service that processes webhook events from Stripe. The service needs to: validate Stripe signatures, look up order records in Cloud SQL, send confirmation emails via SendGrid, and occasionally run a 90-second data reconciliation job. They're evaluating Cloud Functions Gen 2 vs Cloud Run.",
      displayFields: [
        { label: "Webhook Processing Time", value: "~500ms average", emphasis: "normal" },
        { label: "Reconciliation Job Duration", value: "~90 seconds", emphasis: "warn" },
        { label: "Max Cloud Functions Timeout", value: "60 minutes (Gen 2)", emphasis: "normal" },
        { label: "Cloud SQL Connection", value: "Required (connection pool needed)", emphasis: "warn" },
        { label: "Expected Traffic", value: "200–2,000 req/hour (variable)", emphasis: "normal" },
        { label: "Team Preference", value: "Minimal infrastructure management", emphasis: "normal" },
      ],
      actions: [
        { id: "cloud-functions-gen2", label: "Use Cloud Functions Gen 2 for webhooks; separate Pub/Sub-triggered function for reconciliation", color: "green" },
        { id: "cloud-run-only", label: "Use a single Cloud Run service for both webhooks and reconciliation", color: "blue" },
        { id: "cloud-functions-only", label: "Use a single Cloud Functions Gen 2 with a 90-second timeout for all operations", color: "yellow" },
        { id: "app-engine-flex", label: "Use App Engine Flexible for always-on connection pooling", color: "orange" },
      ],
      correctActionId: "cloud-functions-gen2",
      rationales: [
        { id: "r-separation-concerns", text: "Separating webhook handling (fast, event-driven) from reconciliation (long-running, scheduled) follows single-responsibility principles and allows each to scale independently." },
        { id: "r-cloud-run-valid", text: "Cloud Run is a valid choice but requires managing a container and always-on minimum instances for Cloud SQL connection pooling. Cloud Functions Gen 2 supports Cloud SQL Proxy natively." },
        { id: "r-single-function-bad", text: "A single Cloud Functions instance handling both fast webhooks and 90-second reconciliation jobs would block webhook concurrency during reconciliation runs." },
        { id: "r-app-engine-legacy", text: "App Engine Flexible is a legacy approach — it requires always-on instances and significantly more configuration than Cloud Functions or Cloud Run." },
        { id: "r-gen2-features", text: "Cloud Functions Gen 2 supports up to 60-minute timeouts, Cloud SQL connections, and Eventarc triggers — making it fully capable for this use case." },
      ],
      correctRationaleId: "r-separation-concerns",
      feedback: {
        perfect: "Excellent design decision! Separating concerns by function matches Cloud Functions' serverless model — each function scales independently and neither blocks the other.",
        partial: "Cloud Run works too, but the team wants minimal infrastructure management. Cloud Functions Gen 2 with separate functions for each concern is simpler and equally capable.",
        wrong: "Mixing a fast webhook handler with a 90-second reconciliation job in a single function creates blocking issues. Separate them: HTTP-triggered function for webhooks, Pub/Sub-triggered function for reconciliation.",
      },
    },
  ],
  hints: [
    "Cloud Functions Gen 2 is built on Cloud Run under the hood — it supports up to 32 GB memory, 60-minute timeouts, and concurrency (multiple requests per instance). Choose Gen 2 for production workloads.",
    "Cold starts are caused by min-instances=0. The cost of min-instances=1 in Cloud Functions is typically $5–15/month — almost always worth it for customer-facing functions with latency SLAs.",
    "Use the native trigger type that matches your event source: GCS → Cloud Storage trigger, Pub/Sub → Pub/Sub trigger, Firestore changes → Firestore trigger. Avoid manual HTTP wrappers for event-driven patterns.",
  ],
  scoring: {
    maxScore: 100,
    hintPenalty: 5,
    penalties: { perfect: 0, partial: 10, wrong: 20 },
    passingThresholds: { pass: 80, partial: 50 },
  },
  careerInsight:
    "Serverless functions are a foundational skill for modern backend and platform engineers. Cloud Functions expertise is particularly valued in startups and scale-ups that want fast iteration without managing servers. Understanding when NOT to use Cloud Functions (and choosing Cloud Run instead) demonstrates architectural maturity that senior engineers look for in interviews.",
  toolRelevance: ["GCP Console (Cloud Functions)", "gcloud functions CLI", "Cloud Logging", "Eventarc", "Cloud Run"],
  createdAt: "2026-03-28",
  updatedAt: "2026-03-28",
};
