import type { LabManifest } from "../../types/manifest";

export const gcpCloudRunScalingLab: LabManifest = {
  schemaVersion: "1.1",
  id: "gcp-cloud-run-scaling",
  version: 1,
  title: "Cloud Run Scaling Configuration",
  tier: "beginner",
  track: "gcp-essentials",
  difficulty: "moderate",
  accessLevel: "free",
  tags: ["gcp", "cloud-run", "autoscaling", "concurrency", "serverless", "containers"],
  description:
    "Configure Cloud Run scaling parameters — concurrency, min/max instances, CPU allocation, and request timeouts — to balance performance, cost, and reliability for production workloads.",
  estimatedMinutes: 12,
  learningObjectives: [
    "Set appropriate concurrency values based on application thread-safety and memory footprint",
    "Configure min-instances to eliminate cold starts for latency-sensitive services",
    "Choose between CPU always-allocated and CPU throttled based on workload patterns",
    "Apply request timeout and max-instances to prevent runaway scaling costs",
  ],
  sortOrder: 306,
  status: "published",
  prerequisites: [],
  rendererType: "action-rationale",
  scenarios: [
    {
      type: "action-rationale",
      id: "cr-scenario-1",
      title: "High Concurrency Misconfiguration",
      context:
        "A Cloud Run service handling REST API requests is experiencing high p99 latency (8+ seconds) despite low average CPU utilization (15%). The service is Node.js with async/await throughout. Cloud Run metrics show very few instances spinning up despite 2,000+ concurrent requests.",
      displayFields: [
        { label: "Runtime", value: "Node.js 20", emphasis: "normal" },
        { label: "Current Concurrency", value: "1 (one request per instance)", emphasis: "critical" },
        { label: "Active Instances", value: "~40 (at peak)", emphasis: "warn" },
        { label: "Concurrent Requests (peak)", value: "2,000+", emphasis: "warn" },
        { label: "Avg CPU per instance", value: "15%", emphasis: "normal" },
        { label: "p99 Latency", value: "8.2 seconds", emphasis: "critical" },
        { label: "Memory per instance", value: "256 MB (current)", emphasis: "normal" },
      ],
      actions: [
        { id: "set-concurrency-80", label: "Set concurrency to 80 (Cloud Run default for HTTP)", color: "green" },
        { id: "set-concurrency-1000", label: "Set concurrency to 1000 (maximum)", color: "yellow" },
        { id: "increase-memory-2gb", label: "Increase memory to 2 GB to handle more concurrent requests", color: "orange" },
        { id: "increase-max-instances", label: "Increase max-instances from 40 to 1000", color: "blue" },
        { id: "move-to-gke", label: "Migrate to GKE for fine-grained concurrency control", color: "red" },
      ],
      correctActionId: "set-concurrency-80",
      rationales: [
        { id: "r-concurrency-node", text: "Node.js is single-threaded but non-blocking — it handles many concurrent I/O-bound requests efficiently. Concurrency=1 forces Cloud Run to spin one instance per request, defeating the purpose of async Node.js." },
        { id: "r-concurrency-80", text: "Cloud Run's default concurrency of 80 means one instance can handle up to 80 simultaneous requests. For async Node.js with 15% CPU utilization, 80 concurrent requests per instance is appropriate and reduces instance count from 2000 to ~25." },
        { id: "r-concurrency-1000-risky", text: "Concurrency=1000 risks memory exhaustion — 1000 in-flight requests each holding state can exhaust 256 MB easily. Start with 80 and tune upward." },
        { id: "r-max-instances-treats-symptom", text: "Adding more instances at concurrency=1 would require 2,000 instances for 2,000 requests — extreme cost. Fix concurrency first." },
        { id: "r-gke-overkill", text: "GKE adds significant operational overhead (node pools, pod autoscaling, cluster management). The problem is a single configuration value in Cloud Run — fix it there first." },
      ],
      correctRationaleId: "r-concurrency-node",
      feedback: {
        perfect: "Exactly right! Concurrency=1 means one instance per request. For async Node.js, setting concurrency to 80 lets each instance handle 80 simultaneous requests, reducing instances from 2,000 to ~25.",
        partial: "You addressed the scaling issue, but the root cause is concurrency=1. Node.js is perfectly suited for high concurrency — setting it to 80 aligns Cloud Run's behavior with the application's actual capabilities.",
        wrong: "With concurrency=1, Cloud Run creates one instance per request regardless of application capacity. Node.js async code can handle 80+ concurrent requests per instance — fix the concurrency setting.",
      },
    },
    {
      type: "action-rationale",
      id: "cr-scenario-2",
      title: "Batch Processing: CPU Allocation Strategy",
      context:
        "A Cloud Run service processes daily batch data transformation jobs triggered by a Pub/Sub message. Each job takes 3–5 minutes and is heavily CPU-bound (NumPy/pandas transformations). Between jobs, the service is idle for hours. The team wants to minimize billing while ensuring jobs complete in under 10 minutes.",
      displayFields: [
        { label: "Workload Type", value: "CPU-bound batch (NumPy/pandas)", emphasis: "normal" },
        { label: "Job Duration", value: "3–5 minutes", emphasis: "normal" },
        { label: "Idle Time Between Jobs", value: "Several hours", emphasis: "normal" },
        { label: "Current CPU Allocation", value: "CPU always allocated", emphasis: "warn" },
        { label: "Current Monthly Cost", value: "$340 (paying for idle time)", emphasis: "warn" },
        { label: "CPU per instance", value: "2 vCPU", emphasis: "normal" },
      ],
      actions: [
        { id: "cpu-throttled", label: "Switch to CPU throttled (only allocated during request processing)", color: "green" },
        { id: "min-instances-0", label: "Set min-instances to 0 with CPU always-allocated", color: "yellow" },
        { id: "cloud-run-jobs", label: "Migrate to Cloud Run Jobs instead of Cloud Run Services", color: "blue" },
        { id: "increase-cpu-4vcpu", label: "Increase to 4 vCPU to complete jobs faster, reducing compute time billed", color: "orange" },
      ],
      correctActionId: "cloud-run-jobs",
      rationales: [
        { id: "r-run-jobs-batch", text: "Cloud Run Jobs are purpose-built for batch workloads. They run to completion, support parallelism, and are billed only for execution time. A Pub/Sub trigger can launch a Job via Cloud Scheduler or a Cloud Functions trigger." },
        { id: "r-cpu-throttled-wrong", text: "CPU throttled is correct for request-response services, but batch jobs need full CPU throughout their entire 3–5 minute duration. With CPU throttled, Cloud Run throttles CPU when the instance is not actively handling an HTTP request." },
        { id: "r-min-0-cpu-always-expensive", text: "min-instances=0 with CPU always-allocated means you pay for CPU even when idle — exactly the current problem. This doesn't help." },
        { id: "r-4vcpu-partial", text: "Increasing CPU speeds up the job but doesn't address the architectural mismatch. Cloud Run Services are optimized for stateless HTTP — Cloud Run Jobs are designed for this pattern." },
      ],
      correctRationaleId: "r-run-jobs-batch",
      feedback: {
        perfect: "Excellent! Cloud Run Jobs are the correct tool for batch processing. They run to completion, are triggered on-demand, and are billed only for the execution duration — perfect for Pub/Sub-triggered batch workloads.",
        partial: "You identified a cost issue, but the root problem is using Cloud Run Services (designed for HTTP servers) for a batch workload. Cloud Run Jobs are the correct GCP primitive for this pattern.",
        wrong: "A Cloud Run Service is designed for HTTP request-response workloads. For Pub/Sub-triggered batch processing jobs, Cloud Run Jobs are the correct primitive — they run to completion and bill only for execution time.",
      },
    },
    {
      type: "action-rationale",
      id: "cr-scenario-3",
      title: "Cost Spike from Runaway Scaling",
      context:
        "An e-commerce service on Cloud Run received a burst of automated bot traffic during a sale event. Cloud Run auto-scaled from 5 instances to 950 instances in 2 minutes, costing $2,100 in 4 hours. The legitimate traffic peak requires about 50 instances maximum.",
      displayFields: [
        { label: "Legitimate Peak Instances Needed", value: "~50", emphasis: "normal" },
        { label: "Instances Reached", value: "950", emphasis: "critical" },
        { label: "Cost in 4 Hours", value: "$2,100", emphasis: "critical" },
        { label: "Current max-instances", value: "1000 (default)", emphasis: "warn" },
        { label: "Current min-instances", value: "2", emphasis: "normal" },
        { label: "Bot Traffic Proportion", value: "~94% of requests during incident", emphasis: "critical" },
      ],
      actions: [
        { id: "set-max-instances-100", label: "Set max-instances to 100 and add Cloud Armor bot protection", color: "green" },
        { id: "set-max-instances-50", label: "Set max-instances to 50 only", color: "yellow" },
        { id: "reduce-concurrency-1", label: "Set concurrency to 1 to reduce per-instance cost", color: "red" },
        { id: "set-max-instances-200", label: "Set max-instances to 200 with rate limiting at Cloud Load Balancer", color: "blue" },
      ],
      correctActionId: "set-max-instances-100",
      rationales: [
        { id: "r-cap-plus-armor", text: "Setting max-instances=100 (2× legitimate peak for headroom) caps runaway scaling cost. Cloud Armor bot protection blocks bot traffic before it reaches Cloud Run, addressing both cost and the root cause." },
        { id: "r-max-50-too-tight", text: "50 instances with no headroom risks legitimate user traffic being throttled during actual traffic spikes. 100 provides a safety margin while capping worst-case cost." },
        { id: "r-concurrency-1-wrong", text: "Reducing concurrency makes each instance handle fewer requests, requiring even more instances to serve the same traffic — the opposite of what's needed." },
        { id: "r-rate-limiting-partial", text: "Rate limiting at the load balancer helps but doesn't address bot traffic. Cloud Armor specifically identifies and blocks bot user agents and behavioral patterns, not just rate limits." },
      ],
      correctRationaleId: "r-cap-plus-armor",
      feedback: {
        perfect: "Correct dual approach! max-instances caps the financial exposure from runaway scaling, while Cloud Armor removes the bot traffic that caused the scaling in the first place.",
        partial: "You addressed the cost side. Adding Cloud Armor completes the solution by preventing bot traffic from reaching Cloud Run at all, eliminating the root cause of the scaling event.",
        wrong: "Two problems need fixing: (1) max-instances=1000 with no cap allowed 950 instances — set it to 100 for realistic headroom. (2) 94% bot traffic triggered the scaling — use Cloud Armor to block bots upstream.",
      },
    },
  ],
  hints: [
    "Cloud Run concurrency controls how many simultaneous requests one instance can serve. The correct value depends on your runtime: Node.js/Go async = high concurrency (80+), CPU-bound Python = low concurrency (1–10).",
    "For batch workloads triggered by Pub/Sub, always consider Cloud Run Jobs over Cloud Run Services. Jobs run to completion, support parallelism with task indices, and don't require an HTTP server.",
    "Always set max-instances for production Cloud Run services. The default of 1000 means a traffic spike (or bot attack) can generate a very large bill in minutes. Set max-instances to 2–3× your expected legitimate peak.",
  ],
  scoring: {
    maxScore: 100,
    hintPenalty: 5,
    penalties: { perfect: 0, partial: 10, wrong: 20 },
    passingThresholds: { pass: 80, partial: 50 },
  },
  careerInsight:
    "Cloud Run is GCP's most popular serverless compute product and is heavily used across startups and enterprises alike. Engineers who understand concurrency tuning, CPU allocation modes, and the difference between Cloud Run Services vs Jobs stand out in backend and platform engineering interviews. Cost control via max-instances is a real-world FinOps skill.",
  toolRelevance: ["GCP Console (Cloud Run)", "gcloud run CLI", "Cloud Monitoring", "Cloud Armor", "Cloud Run Jobs"],
  createdAt: "2026-03-28",
  updatedAt: "2026-03-28",
};
