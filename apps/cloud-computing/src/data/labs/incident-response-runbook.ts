import type { LabManifest } from "../../types/manifest";

export const incidentResponseRunbookLab: LabManifest = {
  schemaVersion: "1.1",
  id: "incident-response-runbook",
  version: 1,
  title: "Incident Response Runbook",
  tier: "beginner",
  track: "cloud-operations",
  difficulty: "moderate",
  accessLevel: "free",
  tags: ["sre", "incident-response", "runbook", "on-call", "escalation"],
  description:
    "Triage and respond to production incidents by correctly classifying severity, choosing the right remediation steps, and following established runbook procedures to minimize customer impact.",
  estimatedMinutes: 12,
  learningObjectives: [
    "Classify incident severity using SEV levels based on customer impact",
    "Select the correct first responder action to contain an incident",
    "Distinguish between immediate mitigation and root cause investigation",
    "Identify when to escalate versus self-resolve an incident",
  ],
  sortOrder: 604,
  status: "published",
  prerequisites: [],
  rendererType: "triage-remediate",
  scenarios: [
    {
      type: "triage-remediate",
      id: "ir-s1",
      title: "Payment Service Timeout Spike",
      description:
        "PagerDuty fires at 2:17 AM. CloudWatch shows the payment service Lambda function timeout rate jumped from 0.1% to 34% over the last 5 minutes. Stripe webhook processing is failing. Customers are unable to complete purchases. The Lambda function calls an RDS PostgreSQL database.",
      evidence: [
        { type: "metric", content: "Lambda Timeout Rate: 34% (normal: <0.5%)" },
        { type: "metric", content: "Lambda Duration P99: 29,800ms (timeout configured at 30,000ms)" },
        { type: "metric", content: "RDS DatabaseConnections: 498/500 (max_connections: 500)" },
        { type: "log", content: "[ERROR] Connection timeout after 29s: could not connect to database" },
        { type: "log", content: "[ERROR] FATAL: remaining connection slots are reserved for replication" },
        { type: "metric", content: "RDS CPUUtilization: 12% (normal)" },
        { type: "metric", content: "Active Stripe Webhooks Queued: 4,312 (growing)" },
      ],
      classifications: [
        { id: "sev1-db-saturation", label: "SEV-1: Database connection pool exhaustion causing payment failure", description: "Critical — all customers unable to pay, active connection saturation confirmed." },
        { id: "sev2-lambda-bug", label: "SEV-2: Lambda code regression causing slow queries", description: "High — payments degraded but partial functionality may remain." },
        { id: "sev3-stripe-outage", label: "SEV-3: External Stripe service degradation", description: "Medium — third-party issue, limited internal action possible." },
        { id: "sev2-network", label: "SEV-2: Network connectivity issue between Lambda and RDS", description: "High — transient network disruption causing connection failures." },
      ],
      correctClassificationId: "sev1-db-saturation",
      remediations: [
        { id: "restart-rds", label: "Reboot the RDS instance to reset all connections", description: "Forces all 498 connections to drop and allows the service to reconnect." },
        { id: "add-pgbouncer", label: "Deploy PgBouncer connection pooler in front of RDS immediately", description: "Long-term fix but takes 30+ minutes to configure and deploy under pressure." },
        { id: "kill-idle-connections", label: "Run SELECT pg_terminate_backend() to kill idle connections, then scale Lambda concurrency down temporarily", description: "Immediate surgical fix: free connections by terminating idle ones, reduce new connection creation rate." },
        { id: "increase-max-connections", label: "Increase RDS max_connections parameter and reboot", description: "Requires a parameter group change and instance reboot, taking 5–10 minutes and causing additional downtime." },
      ],
      correctRemediationId: "kill-idle-connections",
      rationales: [
        { id: "r-ir-s1", text: "The evidence is unambiguous: 498/500 connections used, Lambda timeouts at 29.9 seconds waiting for a connection, and error messages confirming connection slot exhaustion. Killing idle connections immediately frees slots, while throttling Lambda concurrency prevents the pool from re-saturating before a permanent fix (PgBouncer) can be deployed." },
        { id: "r-ir-s1b", text: "Rebooting the RDS instance would reset connections but causes several minutes of complete database downtime during an active payment outage, extending customer impact. The surgical approach of terminating idle connections preserves active transactions while freeing capacity." },
      ],
      correctRationaleId: "r-ir-s1",
      feedback: {
        perfect: "Correct classification and remediation. Killing idle connections + throttling Lambda concurrency is the fastest path to restoring payments without requiring downtime.",
        partial: "You identified the right system but chose a remediation that introduces additional downtime. During a SEV-1 payment outage, rebooting RDS adds minutes of complete unavailability to an already critical incident.",
        wrong: "Review the evidence: RDS CPU is normal (12%), ruling out slow queries. No Stripe status page issues were mentioned. The connection count at 498/500 is the smoking gun — this is database connection exhaustion.",
      },
    },
    {
      type: "triage-remediate",
      id: "ir-s2",
      title: "Deployment-Triggered Memory Leak",
      description:
        "At 3:45 PM, after a deployment to the user-service ECS task, CloudWatch shows memory utilization climbing on all 6 running tasks. Three tasks have already been replaced by ECS health checks. Customer-facing errors are at 2%. The deployment was 45 minutes ago.",
      evidence: [
        { type: "metric", content: "ECS Memory Utilization: 94% and rising (was 55% pre-deploy)" },
        { type: "metric", content: "ECS Task Replacement Rate: 3 tasks replaced in last 20 minutes" },
        { type: "metric", content: "HTTP 5xx Error Rate: 2.1% (normal: 0.1%)" },
        { type: "log", content: "[WARN] Memory usage 87% — approaching container limit" },
        { type: "log", content: "[INFO] Deployment user-service:v2.4.1 completed at 15:00:00" },
        { type: "metric", content: "ECS Task Count: 6/6 desired (health checks replacing crashed tasks)" },
        { type: "event", content: "Previous deployment: user-service:v2.4.0 at 09:00:00 (stable for 6 hours)" },
      ],
      classifications: [
        { id: "sev2-memory-leak-deploy", label: "SEV-2: Deployment-introduced memory leak in user-service v2.4.1", description: "High — progressive degradation correlated with deployment, rollback available." },
        { id: "sev1-infrastructure-failure", label: "SEV-1: Infrastructure failure causing OOM kills across all tasks", description: "Critical — hardware or ECS cluster issue affecting all services." },
        { id: "sev3-traffic-spike", label: "SEV-3: Unexpected traffic spike exhausting container memory", description: "Medium — load-related, not a code defect." },
        { id: "sev2-config-change", label: "SEV-2: Misconfigured memory limit in ECS task definition", description: "High — task definition error causing false OOM conditions." },
      ],
      correctClassificationId: "sev2-memory-leak-deploy",
      remediations: [
        { id: "rollback-deployment", label: "Immediately roll back to user-service:v2.4.0 via ECS service update", description: "Reverts to the last known-good version, stopping the leak at its source within minutes." },
        { id: "increase-memory-limit", label: "Increase the ECS task memory limit from current to 8GB", description: "Buys time but does not fix the leak — memory will continue to grow until tasks OOM again." },
        { id: "restart-tasks", label: "Force restart all 6 ECS tasks to clear current memory state", description: "Temporary relief only — tasks will re-leak after restart since v2.4.1 is still deployed." },
        { id: "enable-swap", label: "Enable swap on the ECS host instances to absorb memory overflow", description: "Swap on containerized workloads causes severe performance degradation and still does not fix the underlying leak." },
      ],
      correctRemediationId: "rollback-deployment",
      rationales: [
        { id: "r-ir-s2", text: "The correlation between v2.4.1 deployment and memory climb is definitive evidence of a deployment-introduced regression. With a known-good version (v2.4.0) available, rollback is always the fastest and safest SEV-2 remediation during business hours. Rollback stops the bleed in minutes; all other options are temporary measures that leave bad code running." },
        { id: "r-ir-s2b", text: "Increasing the memory limit only delays the inevitable OOM without addressing the leak. The tasks will continue consuming memory until they crash again, turning a single incident into a recurring one while the defective code remains deployed." },
      ],
      correctRationaleId: "r-ir-s2",
      feedback: {
        perfect: "Correct. Rollback is the fastest path to stability when a deployment-correlated regression is confirmed and a known-good version is available.",
        partial: "Restarting tasks is correct as a temporary measure but leaves the bad code running. In a real incident, restart buys time while rollback is the definitive fix.",
        wrong: "The timing correlation (memory spike at 3:00 PM matching deployment at 3:00 PM) is strong evidence of a code regression. Always roll back first, investigate second.",
      },
    },
    {
      type: "triage-remediate",
      id: "ir-s3",
      title: "S3 Access Denied — Static Asset Outage",
      description:
        "At 10:22 AM, your company's marketing website goes blank. Browser developer tools show all CSS, JS, and image files returning HTTP 403 Forbidden. The files are served from an S3 bucket with a CloudFront distribution. No deployments have occurred today. An IAM policy review happened yesterday afternoon.",
      evidence: [
        { type: "log", content: "CloudFront access log: 403 Forbidden for s3://assets.company.com/styles/main.css" },
        { type: "log", content: "S3 server access log: 403 AccessDenied — OAI (Origin Access Identity) request rejected" },
        { type: "event", content: "IAM policy change at 16:45 yesterday — S3 bucket policy modified" },
        { type: "metric", content: "CloudFront 4xx Error Rate: 98% (was 0.1%)" },
        { type: "log", content: "AWS CloudTrail: PutBucketPolicy executed by iam-admin@company.com at 16:45:33" },
        { type: "metric", content: "S3 Bucket Objects: 2,341 (unchanged, files still exist)" },
        { type: "event", content: "S3 Block Public Access: Enabled (unchanged)" },
      ],
      classifications: [
        { id: "sev2-bucket-policy-change", label: "SEV-2: S3 bucket policy change broke CloudFront OAI permissions", description: "High — marketing site down, caused by yesterday's IAM policy modification removing OAI access." },
        { id: "sev1-files-deleted", label: "SEV-1: S3 objects deleted or corrupted", description: "Critical — asset files missing or unreadable." },
        { id: "sev3-cloudfront-cache", label: "SEV-3: CloudFront cache invalidation issue serving stale 403 responses", description: "Low — temporary cache issue, expected to self-resolve." },
        { id: "sev2-oai-deleted", label: "SEV-2: CloudFront Origin Access Identity was deleted", description: "High — OAI removed from CloudFront, breaking authorization." },
      ],
      correctClassificationId: "sev2-bucket-policy-change",
      remediations: [
        { id: "restore-bucket-policy", label: "Restore the S3 bucket policy to re-grant s3:GetObject to the CloudFront OAI principal", description: "Directly fixes the permission that was broken by yesterday's policy change." },
        { id: "make-bucket-public", label: "Set the S3 bucket to public-read to bypass OAI and restore access immediately", description: "Restores the site but exposes S3 directly to the internet, removing CDN cost optimization and enabling direct S3 access." },
        { id: "recreate-cloudfront", label: "Delete and recreate the CloudFront distribution with a new OAI", description: "Takes 15–30 minutes to deploy and propagate globally — far too slow for a site-down incident." },
        { id: "invalidate-cache", label: "Run a CloudFront cache invalidation for all paths (/*)", description: "Invalidations remove cached content but do not fix 403 responses originating from S3 permission denials." },
      ],
      correctRemediationId: "restore-bucket-policy",
      rationales: [
        { id: "r-ir-s3", text: "The S3 server access log confirms the OAI request is being rejected, and CloudTrail proves a PutBucketPolicy was executed yesterday evening. The files exist (object count unchanged). The bucket policy is the single changed variable. Restoring the OAI GetObject permission fixes the incident in under 2 minutes without changing the security posture." },
        { id: "r-ir-s3b", text: "Making the bucket public would restore access but removes the OAI-based access control entirely, exposing the bucket directly to the internet and bypassing CloudFront. This trades a permissions misconfiguration for a permanent security regression." },
      ],
      correctRationaleId: "r-ir-s3",
      feedback: {
        perfect: "Correct. CloudTrail identified the change, S3 access logs confirmed the symptom, and the fix is restoring the exact permission that was inadvertently removed.",
        partial: "Cache invalidation is a valid diagnostic step but doesn't fix permission errors. The 403 is originating at S3, not in CloudFront's cache.",
        wrong: "The files still exist (S3 object count unchanged) so deletion is not the cause. Making the bucket public is a security regression. Always use CloudTrail to identify the change event before deciding on remediation.",
      },
    },
  ],
  hints: [
    "In incident triage, look for the changed variable first — deployments, config changes, and policy modifications are the most common incident triggers.",
    "SEV-1 is for active, complete customer-facing outages. SEV-2 is for significant degradation with partial functionality remaining.",
    "The fastest remediation is almost always reverting the change that caused the incident, not engineering a workaround.",
  ],
  scoring: {
    maxScore: 100,
    hintPenalty: 5,
    penalties: { perfect: 0, partial: 10, wrong: 20 },
    passingThresholds: { pass: 80, partial: 50 },
  },
  careerInsight:
    "Incident response skill is one of the top differentiators between junior and senior engineers. The ability to rapidly classify severity, read logs and metrics together, and choose the fastest safe remediation directly translates to reduced mean time to recovery (MTTR) — a core SRE metric every team measures.",
  toolRelevance: ["PagerDuty", "AWS CloudWatch", "AWS CloudTrail", "AWS ECS", "Datadog"],
  createdAt: "2026-03-28",
  updatedAt: "2026-03-28",
};
