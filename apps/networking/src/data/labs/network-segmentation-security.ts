import type { LabManifest } from "../../types/manifest";

const lab: LabManifest = {
  schemaVersion: "1.1",
  id: "network-segmentation-security",
  version: 1,
  title: "Configure Network Segmentation Security",
  tier: "intermediate",
  track: "network-security",
  difficulty: "moderate",
  accessLevel: "free",
  tags: ["segmentation", "vlan", "acl", "firewall-zones", "microsegmentation", "zero-trust"],
  description:
    "Configure network segmentation using VLANs, ACLs, and firewall zones to isolate sensitive assets, limit lateral movement, and enforce least-privilege inter-zone communication.",
  estimatedMinutes: 14,
  learningObjectives: [
    "Configure VLAN assignments and inter-VLAN ACLs to enforce network segmentation",
    "Identify overly permissive inter-zone policies that allow unauthorized lateral movement",
    "Apply the principle of least privilege to firewall zone traffic policies",
    "Understand how proper segmentation limits the blast radius of a security breach",
  ],
  sortOrder: 408,
  status: "published",
  prerequisites: [],
  rendererType: "toggle-config",
  scenarios: [
    {
      type: "toggle-config",
      id: "nss-001",
      title: "VLAN ACL Hardening for PCI Compliance",
      description:
        "A retail company stores payment card data on servers in VLAN 200. A PCI DSS audit found that the cardholder data environment (CDE) is not properly isolated from other VLANs. Review and correct the inter-VLAN ACL configuration on the core Layer 3 switch.",
      targetSystem: "Cisco Catalyst 9300 Core Switch",
      items: [
        {
          id: "item-1",
          label: "VLAN 100 (Users) -> VLAN 200 (CDE) Access",
          detail: "ACL on VLAN 200 SVI: permit ip 10.1.100.0/24 10.1.200.0/24 (ALLOWS all user traffic to CDE)",
          currentState: "permit-all",
          correctState: "deny-all",
          states: ["permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-cde-isolation",
        },
        {
          id: "item-2",
          label: "VLAN 300 (POS Terminals) -> VLAN 200 (CDE) Access",
          detail: "ACL on VLAN 200 SVI: permit tcp 10.1.300.0/24 10.1.200.0/24 eq 443 (POS to payment gateway only)",
          currentState: "permit-https-only",
          correctState: "permit-https-only",
          states: ["permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-pos-access",
        },
        {
          id: "item-3",
          label: "VLAN 200 (CDE) -> VLAN 400 (Internet DMZ) Access",
          detail: "ACL on VLAN 400 SVI: permit ip 10.1.200.0/24 10.1.400.0/24 (CDE has unrestricted DMZ access)",
          currentState: "permit-all",
          correctState: "permit-https-only",
          states: ["permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-cde-egress",
        },
        {
          id: "item-4",
          label: "VLAN 500 (Guest WiFi) -> VLAN 200 (CDE) Access",
          detail: "ACL on VLAN 200 SVI: No explicit rule (implicit deny at end of ACL)",
          currentState: "deny-all",
          correctState: "deny-all",
          states: ["permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-guest-isolation",
        },
      ],
      rationales: [
        { id: "rat-cde-isolation", text: "PCI DSS Requirement 1.3 mandates that the CDE must be isolated from all non-essential network segments. General user workstations have no business need to access payment card servers directly. All user traffic to the CDE must be denied." },
        { id: "rat-pos-access", text: "POS terminals require HTTPS connectivity to the payment gateway in the CDE to process transactions. This is a legitimate, scoped business need that should be allowed with the minimum required protocol (TCP 443 only)." },
        { id: "rat-cde-egress", text: "CDE servers may need to reach the payment processor via the DMZ, but unrestricted access creates a data exfiltration path. Limiting CDE-to-DMZ traffic to HTTPS only restricts the egress to the required protocol for payment processing." },
        { id: "rat-guest-isolation", text: "Guest WiFi must never have any access to the CDE. The implicit deny is correct, but a best practice is to add an explicit deny rule with logging to detect any attempted access from the guest network." },
      ],
      feedback: {
        perfect: "Excellent! You correctly isolated the CDE from user traffic, maintained necessary POS access, restricted CDE egress to HTTPS only, and kept the guest network fully isolated. This configuration meets PCI DSS segmentation requirements.",
        partial: "Some items are correct but the CDE is not fully isolated. Review PCI DSS Requirement 1.3: only systems with a legitimate business need should have any access to the cardholder data environment.",
        wrong: "The CDE must be isolated from all non-essential traffic per PCI DSS. User workstations must be denied access, POS terminals need only HTTPS, and CDE egress must be restricted to required protocols.",
      },
    },
    {
      type: "toggle-config",
      id: "nss-002",
      title: "Firewall Zone Policy for Segmented Network",
      description:
        "A mid-size company uses a Palo Alto firewall with four security zones. After a ransomware incident that spread from the user zone to servers, management requires strict zone-based segmentation. Review the inter-zone policies and correct the configuration.",
      targetSystem: "Palo Alto PA-850 Firewall",
      items: [
        {
          id: "item-1",
          label: "Users Zone -> Servers Zone Policy",
          detail: "Policy: allow application=any, service=any, from=Users to=Servers (unrestricted access to all servers)",
          currentState: "allow-any",
          correctState: "allow-app-id",
          states: ["allow-any", "allow-app-id", "deny-all"],
          rationaleId: "rat-user-server",
        },
        {
          id: "item-2",
          label: "Users Zone -> Users Zone (Intra-zone) Policy",
          detail: "Policy: intrazone-default=allow (workstations can communicate freely with each other)",
          currentState: "allow-any",
          correctState: "deny-all",
          states: ["allow-any", "allow-app-id", "deny-all"],
          rationaleId: "rat-intrazone",
        },
        {
          id: "item-3",
          label: "Servers Zone -> Internet Zone Policy",
          detail: "Policy: allow application=any, service=any, from=Servers to=Internet (servers have unrestricted internet)",
          currentState: "allow-any",
          correctState: "allow-app-id",
          states: ["allow-any", "allow-app-id", "deny-all"],
          rationaleId: "rat-server-internet",
        },
        {
          id: "item-4",
          label: "Management Zone -> All Zones Policy",
          detail: "Policy: allow application=ssh,rdp,https, from=Management to=any (management access restricted to admin protocols)",
          currentState: "allow-app-id",
          correctState: "allow-app-id",
          states: ["allow-any", "allow-app-id", "deny-all"],
          rationaleId: "rat-mgmt-access",
        },
      ],
      rationales: [
        { id: "rat-user-server", text: "Unrestricted Users-to-Servers access allowed ransomware to spread from an infected workstation to file servers. App-ID based policies restrict traffic to only approved applications (e.g., SMB for file shares, HTTPS for web apps, SQL for database clients), blocking unauthorized protocols that malware uses for lateral movement." },
        { id: "rat-intrazone", text: "Allowing workstations to communicate freely within the Users zone enabled the ransomware to spread peer-to-peer via SMB. Denying intra-zone traffic prevents workstation-to-workstation lateral movement, as users rarely need direct connectivity to each other." },
        { id: "rat-server-internet", text: "Unrestricted server-to-internet access allows compromised servers to contact C2 servers and exfiltrate data. App-ID policies should limit server internet access to only required applications (e.g., DNS, NTP, HTTPS for updates)." },
        { id: "rat-mgmt-access", text: "Management zone access is already correctly restricted to SSH, RDP, and HTTPS using App-ID. This provides administrators necessary access while preventing unauthorized protocols from traversing the management plane." },
      ],
      feedback: {
        perfect: "Perfect configuration! You restricted user-to-server traffic to approved applications, blocked intra-zone workstation communication, limited server internet access, and maintained proper management access. This segmentation would have contained the ransomware outbreak.",
        partial: "Some policies are correct, but full containment requires App-ID policies on user-to-server and server-to-internet zones, plus blocking intra-zone workstation traffic to prevent lateral movement.",
        wrong: "The ransomware spread because of unrestricted zone policies. Users-to-Servers needs App-ID filtering, intra-zone user traffic must be denied, and server internet access must be limited to required applications.",
      },
    },
    {
      type: "toggle-config",
      id: "nss-003",
      title: "IoT Network Isolation on Healthcare Network",
      description:
        "A hospital network has medical IoT devices (infusion pumps, patient monitors, imaging systems) sharing the same VLAN as clinical workstations. After a vulnerability scan revealed unpatched IoT devices, the network team must segment IoT devices into a dedicated VLAN with appropriate access controls.",
      targetSystem: "Cisco Catalyst 9500 + ISE",
      items: [
        {
          id: "item-1",
          label: "IoT VLAN -> Clinical Workstations VLAN Access",
          detail: "Current: IoT devices on same VLAN 10 as workstations (no segmentation). Proposed: Move IoT to VLAN 50 with ACL.",
          currentState: "same-vlan",
          correctState: "deny-all",
          states: ["same-vlan", "permit-all", "deny-all"],
          rationaleId: "rat-iot-workstation",
        },
        {
          id: "item-2",
          label: "IoT VLAN -> EMR Server Access",
          detail: "Infusion pumps and patient monitors must send HL7 FHIR data to the EMR server (10.10.1.50) on TCP 443.",
          currentState: "same-vlan",
          correctState: "permit-https-only",
          states: ["same-vlan", "permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-iot-emr",
        },
        {
          id: "item-3",
          label: "IoT VLAN -> Internet Access",
          detail: "Some imaging systems require firmware updates from vendor cloud portals via HTTPS.",
          currentState: "same-vlan",
          correctState: "permit-https-only",
          states: ["same-vlan", "permit-all", "permit-https-only", "deny-all"],
          rationaleId: "rat-iot-internet",
        },
        {
          id: "item-4",
          label: "IoT VLAN -> IoT VLAN (Intra-VLAN) Access",
          detail: "IoT devices on VLAN 50 can communicate with each other freely by default.",
          currentState: "permit-all",
          correctState: "deny-all",
          states: ["permit-all", "deny-all"],
          rationaleId: "rat-iot-intra",
        },
      ],
      rationales: [
        { id: "rat-iot-workstation", text: "Unpatched IoT devices pose a direct risk to clinical workstations on the same VLAN. Segmenting IoT devices into VLAN 50 with a deny-all ACL to the workstation VLAN prevents a compromised infusion pump or monitor from being used as a pivot point to attack clinical systems." },
        { id: "rat-iot-emr", text: "Medical IoT devices have a legitimate need to transmit patient data to the EMR server via HL7 FHIR over HTTPS. This specific path (TCP 443 to the EMR server IP only) should be permitted while all other traffic from the IoT VLAN to the server VLAN is denied." },
        { id: "rat-iot-internet", text: "Imaging systems require vendor cloud access for firmware updates. Restricting IoT internet access to HTTPS only (with URL filtering to vendor domains) allows necessary updates while blocking unauthorized outbound communication that could indicate compromise." },
        { id: "rat-iot-intra", text: "IoT devices rarely need to communicate with each other. Blocking intra-VLAN IoT traffic prevents a compromised device from scanning or attacking other IoT devices on the same segment, limiting the blast radius of any single device compromise." },
      ],
      feedback: {
        perfect: "Excellent segmentation! You isolated IoT devices from workstations, allowed only necessary EMR and update traffic, and blocked intra-VLAN IoT communication. This limits the blast radius of any compromised medical device.",
        partial: "The IoT devices are partially segmented, but review whether all access paths follow least privilege. IoT devices should only reach the EMR server and update servers, with no workstation or intra-VLAN access.",
        wrong: "Unpatched IoT devices on the same VLAN as clinical workstations is a critical risk. They must be segmented into a dedicated VLAN with access restricted to only the EMR server (HTTPS) and vendor update portals.",
      },
    },
  ],
  hints: [
    "PCI DSS requires the cardholder data environment to be isolated from all network segments that do not have a business need for access.",
    "After a ransomware incident, focus on blocking both lateral movement between zones and intra-zone workstation-to-workstation communication.",
    "Medical IoT devices are frequently unpatched and should be segmented into their own VLAN with strict ACLs limiting access to only required clinical systems.",
  ],
  scoring: {
    maxScore: 100,
    hintPenalty: 5,
    penalties: { perfect: 0, partial: 10, wrong: 20 },
    passingThresholds: { pass: 80, partial: 50 },
  },
  careerInsight:
    "Network segmentation is a top priority in enterprise security and compliance frameworks (PCI DSS, HIPAA, NIST). Employers expect network engineers and security architects to design and implement segmentation using VLANs, ACLs, and zone-based firewalls.",
  toolRelevance: ["Cisco ISE", "Palo Alto NGFW", "Cisco Catalyst", "802.1Q VLANs", "ACLs"],
  createdAt: "2026-03-28",
  updatedAt: "2026-03-28",
};

export default lab;
